name: Request bundle

on:
  issues:
    types:
      - opened
      - reopened

jobs:
  request-bundle:
    runs-on: macos-11
    steps:
      - uses: actions/checkout@v2
      - name: Autobundle Bot
        uses: ./autobundle-bot
